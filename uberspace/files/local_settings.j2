ALLOWED_HOSTS = ['{{ domain }}']
DEBUG = False

DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.mysql',
            'NAME': '{{ ansible_user }}_hagrid',
            'USER': '{{ ansible_user }}',
            'PASSWORD': '{{ db_password }}',
            'HOST': 'localhost',
            }
        }

CACHES = {
    'default': {
        'BACKEND': 'django.core.cache.backends.db.DatabaseCache',
        'LOCATION': 'hagrid_cache_default',
    }
}

SITE_URL = 'https://{{ domain }}'

STATIC_ROOT = "/var/www/virtual/{{ ansible_user }}/{{ domain }}/static/"
MEDIA_ROOT = "/var/www/virtual/{{ ansible_user }}/{{ domain }}/media/"

SESSION_COOKIE_SECURE = True
CSRF_COOKIE_SECURE =  True
SECURE_BROWSER_XSS_FILTER = True
X_FRAME_OPTIONS = 'DENY'

SECRET_KEY = "{{ django_secret }}"

ADMINS = [('Admin', '{{ admin_mail }}')]
DEFAULT_FROM_EMAIL = SERVER_EMAIL = MAIL_FROM = "no-reply@{{ domain }}"
EMAIL_HOST = "{{ inventory_hostname }}"
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = "{{ ansible_user }}@uber.space"
EMAIL_HOST_PASSWORD = "{{ email_password }}"
